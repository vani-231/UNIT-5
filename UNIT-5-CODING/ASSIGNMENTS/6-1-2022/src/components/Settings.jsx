import { Flex, Heading } from "@chakra-ui/react";

export const Settings = () => {
    return (
        <Flex justify={"center"} mt={20}>
            <Heading>Welcome to Settings Page</Heading>
        </Flex>
    );
};